#include<stdio.h>

main(){
	int a = 80,b = 180;
	//Here we print the decimal values in octal and hexadecimal values
	printf("%d in octal is %o , %d in hexadecimal is %x",a,a,a,a);
	printf("\n%d in octal is %o, %d in hexadecimal is %x",b,b,b,b);
	getch();
}
