#include<stdio.h>

main(){
	int i;
	//we assigning the multiple values to a single variable with parenthesis
	i = (10,20,30);
	//can we add add parenthesis
	//printing the variable 'i' 
	printf("%d",i);
	//print the the last value of the variable
	getch();
}
