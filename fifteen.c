#include<stdio.h>

main(){
	//here we the assign the single value to different varaibles
	int x,y = 5, z = 5;
	x = y == z; // we assign the y value to x variable 
	//and compare the x and z value 
	//if its true assign 1 value to a 'x' variable and else return 0 value assigned
	printf("%d", x); //print the x value
	getch();
}
